apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "popper.fullname" . }}-config
  labels:
    {{- include "popper.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
